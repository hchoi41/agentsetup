---
name: wrap-up
description: Wrap up the current session and prepare for continuation in a new chat (same machine or a different device)
---

Run the **WRAP-UP phase** of the session-handoff skill. Read `SKILL.md` in the `session-handoff` skill directory and execute Phase 1 in order. Make sure everything needed to resume is written to the Synced (cross-device) tier — the RAM and the stable `00.ABOUT/SESSION_HANDOFF.md` pointer — not only to device-local auto-memory.
